# GridSignals runtime dependencies.
#
# The project is stdlib-only by rule (urllib + xml.etree + sqlite3), with ONE
# approved exception: Streamlit for the UI layer (app/ui/ only). Everything
# outside app/ui/ imports nothing from here. No component libraries
# (Plotly/echarts/aggrid/pydeck) at this stage - R8.1 cards are custom
# HTML/CSS per R8.9.
# Pinned (not >=1.57,<2) so the deployed image and CI use the exact version the
# app is tested against. A newer 1.x changed AppTest.from_file relative-path
# resolution, which broke the UI tests under the CI-installed version; pinning
# keeps the artifact reproducible. Bump deliberately, re-running the suite.
streamlit==1.57.0
